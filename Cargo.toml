[package]
name = "viua"
version = "0.1.0"
edition = "2021"
description = "Convert images to ASCII art in the terminal"
license = "MIT"
readme = "README.md"
exclude = ["/.github"]


[dependencies]
image = "0.25"
clap = { version = "4.4", features = ["cargo"] }
crossterm = "0.27"
viuer = "0.11"